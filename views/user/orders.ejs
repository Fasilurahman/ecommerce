<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/header') %>
    <title>Your Orders</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- Include Font Awesome -->
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            min-height: 100vh;
            background:  radial-gradient(circle at top right, #eae5e4, #ffffff, #ffffff); /* Modern gradient background */
        }

        .navbar {
            width: 250px;
            background-color: rgba(255, 255, 255, 0.1);
            color: #fff;
            display: flex;
            flex-direction: column;
            height: 100vh;
            padding-top: 2rem;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
            position: fixed;
            backdrop-filter: blur(10px); /* Frosted glass effect */
            border: 1px solid rgba(255, 255, 255, 0.3); /* Subtle border */
        }

        .navbar a {
            color: #887f7f;
            text-decoration: none;
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            font-size: 16px;
            transition: background-color 0.3s, color 0.3s;
            border-left: 4px solid transparent;
            border-radius: 8px;
        }

        .navbar a:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: #0c151f;
        }

        .navbar a.active {
            background-color: #2774d8;
            color: #fff;
            border-left: 4px solid #fff;
        }

        .content {
            margin-left: 250px;
            padding: 2rem;
            background-color: rgba(255, 255, 255, 0.1);
            width: calc(100% - 250px);
            max-width: calc(100% - 250px);
        }

        h1 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 2rem;
        }

        .orders-container {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .order-item {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            border: 1px solid #ddd;
            transition: box-shadow 0.3s;
        }

        .order-item:hover {
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .order-item h4 {
            margin: 0;
            font-size: 1.5rem;
            color: #333;
        }

        .order-item p {
            margin: 0.5rem 0;
            color: #555;
        }

        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            border-radius: 30px;
            text-align: center;
            text-decoration: none;
            font-size: 16px;
            transition: background-color 0.3s;
            border: none;
            cursor: pointer;
            margin-top: 1rem;
        }

        .btn-primary {
            background-color: #007bff;
            color: #fff;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-secondary {
            background-color: #6c757d;
            color: #fff;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        @media (max-width: 768px) {
            .navbar {
                width: 100%;
                height: auto;
                position: relative;
                box-shadow: none;
            }

            .content {
                margin-left: 0;
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="/profile"><i class="fas fa-user">&nbsp;&nbsp;</i> Profile</a>
        <a href="/addresses"><i class="fas fa-address-book">&nbsp;&nbsp;</i> Addresses</a>
        <a href="/orders" class="active"><i class="fas fa-box">&nbsp;&nbsp;</i> Orders</a>
        <a href="/changepassword"><i class="fas fa-key">&nbsp;&nbsp;</i> Change Password</a>
        <li><a href="/wallet"><i class="fas fa-wallet">&nbsp;&nbsp;</i>   Wallet</a></li>
    </nav>
    <div class="content">
        <h1>Your Orders</h1>
        <div class="orders-container">
            <% if (orders && orders.length > 0) { %>
                <% orders.forEach(order => { %>
                    <div class="order-item">
                        <h4>Order #<%= order.orderId %></h4>
                        <p>Date: <%= new Date(order.createdOn).toDateString() %></p>
                        <p>Total: $<%= order.finalPrice.toFixed(2) %></p>
                        <p>Status: <%= order.status %></p>
    
                        <a href="/orders/<%= order._id %>" class="btn btn-primary">View Details</a>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No orders available.</p>
            <% } %>
        </div>
    
        <a href="/profile" class="btn btn-secondary">Back to Profile</a>
    </div>
    
    <%- include('../partials/footer') %>
</body>
</html>
